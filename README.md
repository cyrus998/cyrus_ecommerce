to run

composer install

npm install

npm run dev


create db "furnitura"

php artisan migrate

php artisan db:seed --class=ProductSeeder

php artisan storage:link     // para ito sa profile pic storage

php artisan serve
